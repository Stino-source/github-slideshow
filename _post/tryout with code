System.out.println("---- New Subject: Arrays ----"); //store a list of something
        int [] numbers = new int[5]; // we need those [] after int to make it an array and need to make a new int
        numbers [0] = 5;
        numbers [1] = 4;
        numbers [2] = 3;
        numbers [3] = 2;
        numbers [4] = 1;
        System.out.println(numbers);// retursn a random result calculated on the place were it is stored in memory
        System.out.println(Arrays.toString(numbers));//so we need to convert it with a class
        // new way of writing is just to put all numbers in the list like this int [] numbers = {1,2,3,4,5}
        // to sort random numbers use Arrays.sort(numbers) between brackets whatever name was given
